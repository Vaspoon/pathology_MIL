data:
  root_dir: "D:/Data_CHU/data_bloc_224/HE/"
  output_dir: "D:/Embeddings/Data_CHU/CNN_embeddings/HE/"

model:
  embedding_dim: 256
  weights_path: null   # or "checkpoints/model.pth"

dataloader:
  batch_size: 64
  num_workers: 4
  pin_memory: true

transforms:
  resize: 224
  mean: [0.5, 0.5, 0.5]
  std: [0.5, 0.5, 0.5]

runtime:
  device: "cuda"   # or "cpu"